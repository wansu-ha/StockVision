# Stability 구현 리포트

> 작성일: 2026-03-14 | 브랜치: feat/trading-safety

---

## 구현 결과 요약

모든 8개 스텝 구현 완료.

---

## 스텝별 결과

### ST-4: scheduler.py authenticate → connect
- `cloud_server/collector/scheduler.py:157` — `broker.authenticate()` → `broker.connect()`
- 상태: 완료

### ST-6: password_hash nullable
- `cloud_server/models/user.py:29` — `nullable=False` → `nullable=True`
- `cloud_server/alembic/versions/a1b2c3d4e5f6_password_hash_nullable.py` — 마이그레이션 신규 생성
  - upgrade: 빈 문자열 → NULL 정리 + NOT NULL 제약 제거
  - downgrade: NULL → 빈 문자열 + NOT NULL 복구
- `cloud_server/services/oauth_service.py:145` — `password_hash=""` → `password_hash=None`
- 상태: 완료

### ST-5: OAuth IntegrityError 처리
- `cloud_server/services/oauth_service.py` — `db.flush()` try/except IntegrityError 추가
  - IntegrityError 발생 시 rollback 후 이메일로 기존 사용자 재조회
  - 이메일 외 unique 제약 위반이면 예외 재발생
- `sqlalchemy.exc.IntegrityError` import 추가
- 상태: 완료

### ST-7: Kakao 빈 이메일 차단
- `cloud_server/services/oauth_service.py` `login_or_register()` 진입부에 빈 이메일 체크 추가
  - `not email` → HTTPException(400, "이메일 동의가 필요합니다...")
- `fastapi.HTTPException` import 추가
- 상태: 완료

### ST-3: LogDB async_write 래퍼
- `local_server/storage/log_db.py` — `async_write()` 메서드 추가 (`asyncio.to_thread` 래핑)
- `local_server/engine/executor.py` — 모든 `db.write()` → `await db.async_write()` (async 함수 내 9개 호출)
- `local_server/routers/trading.py` — 모든 `log_db.write()` → `await log_db.async_write()` (async 핸들러 내 5개 호출)
- 상태: 완료

### ST-8: AuthContext loginWithTokens + OAuthCallback
- `frontend/src/context/AuthContext.tsx`
  - `AuthContextValue` 인터페이스에 `loginWithTokens: (jwt, rt) => Promise<void>` 추가
  - `loginWithTokens` useCallback 구현 (sessionStorage/localStorage 저장 + localAuth.setAuthToken + setState)
  - Provider value에 `loginWithTokens` 포함
- `frontend/src/pages/OAuthCallback.tsx`
  - `useAuth()` import + `loginWithTokens` 구조분해
  - `.then()` → `async .then()` + `await loginWithTokens(...)` 호출
  - useEffect deps에 `loginWithTokens` 추가
- 상태: 완료

### ST-2: KisWS disconnect 콜백 + StateMachine 전환
- `local_server/broker/kis/ws.py`
  - `__init__` 파라미터에 `on_disconnect: Callable | None = None` 추가
  - `_recv_loop()` ConnectionClosed 및 일반 Exception 핸들러에서 `_on_disconnect()` 호출
- `local_server/broker/kis/adapter.py`
  - `asyncio` import 추가
  - `KisWS(self._auth, on_disconnect=self._on_ws_disconnect)` 전달
  - `_on_ws_disconnect()` 메서드 추가 — `asyncio.create_task(self._state.transition(ERROR))`
- 상태: 완료

### ST-1: _subscribed_symbols + 재연결 후 재구독
- `local_server/broker/kis/adapter.py`
  - `self._subscribed_symbols: set[str] = set()` 초기화
  - `subscribe_quotes()` — `self._subscribed_symbols.update(symbols)` 추가
  - `_do_connect()` — WS 연결 후 `_subscribed_symbols` 있으면 `ws.subscribe()` 재호출
- 상태: 완료

---

## 검증 결과

| 검증 | 결과 |
|------|------|
| `python -c "from local_server.main import app"` | `local OK` |
| `python -c "from cloud_server.main import app"` | `cloud OK` |
| `npm run build` (frontend) | 성공 (빌드 3151 모듈) |
| `pytest local_server/tests/ -q` | 122 passed, 1 failed (기존 실패) |
| `pytest cloud_server/tests/ -q` | 59 passed, 0 failed |

### 로컬 테스트 실패 분석

`test_loss_lock_notifies_via_callback` — **기존 실패** (이번 변경과 무관).

원인: 테스트에서 `engine._notify_loss_lock()`을 동기 호출하는데, 이 메서드가 코루틴(`async def`)으로 되어 있어 `RuntimeWarning: coroutine 'StrategyEngine._notify_loss_lock' was never awaited` 발생. git stash로 원래 코드로 되돌려도 동일하게 실패 확인.

---

## 변경 파일 목록

```
cloud_server/collector/scheduler.py          ST-4
cloud_server/models/user.py                  ST-6
cloud_server/alembic/versions/a1b2c3d4e5f6_password_hash_nullable.py  ST-6 (신규)
cloud_server/services/oauth_service.py       ST-5, ST-6, ST-7
local_server/storage/log_db.py               ST-3
local_server/engine/executor.py              ST-3
local_server/routers/trading.py              ST-3
frontend/src/context/AuthContext.tsx         ST-8
frontend/src/pages/OAuthCallback.tsx         ST-8
local_server/broker/kis/ws.py                ST-2
local_server/broker/kis/adapter.py           ST-1, ST-2
```
