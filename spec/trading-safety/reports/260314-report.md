# Trading Safety — 구현 리포트

> 날짜: 2026-03-14 | 브랜치: feat/trading-safety

## 구현 결과

| Step | 항목 | 상태 | 비고 |
|------|------|------|------|
| 1 | TS-2: alerts 인증 | 완료 | GET/PUT에 `Depends(require_local_secret)` 추가 |
| 2 | TS-6: Watchdog 엔진 주입 | 완료 | `start_strategy()`에서 `watchdog.set_engine(engine)` 호출 |
| 3 | TS-7: 수동 주문 safeguard | 완료 | `place_order()` 진입부에 `safeguard.is_trading_enabled()` 체크 |
| 4 | TS-1: Kill Switch 트레이 | 완료 | `_on_kill_switch()` → API 호출, `_call_engine_api()`에 `json_body` 파라미터 추가 |
| 5 | TS-4,5: LimitChecker 복원+리셋 | 완료 | `restore_from_db()`, `check_date_boundary()`, `today_executed_amount()` 신규 |
| 6 | TS-3: FILL 로그 타이밍 | 완료 | Option B 선택 — 로그 메시지를 "주문 제출 완료"로 명확화 |
| 7 | TS-8: KIS 모의투자 TR ID | 완료 | `_MOCK_ORDER_TR_ID` 추가, 취소/미체결 조회 TR ID도 모의 분기 |
| 8 | TS-9: KIS 매도 시장가 TR ID | 이슈 해제 | `TTTC0801U`는 통합주문 API, `ORD_DVSN`으로 구분 — 정상 |

## 추가 수정

- `log_db.py` `_init_db()`: intent_id 인덱스 생성을 마이그레이션 후로 이동 (기존 DB 호환성 버그 수정)

## 검증 결과

- import 체크: `from local_server.main import app` — 성공
- pytest: 122 passed, 1 failed (pre-existing: `test_loss_lock_notifies_via_callback` — await 누락 테스트 버그)
- trading-safety 변경으로 인한 regression 없음

## 수정 파일

| 파일 | 변경 |
|------|------|
| `local_server/routers/alerts.py` | `Depends(require_local_secret)` 추가 |
| `local_server/routers/trading.py` | watchdog 주입, safeguard 체크 |
| `local_server/tray/tray_app.py` | `_on_kill_switch` API 호출, `_call_engine_api` json_body |
| `local_server/engine/limit_checker.py` | `restore_from_db()`, `check_date_boundary()`, `_last_date` |
| `local_server/engine/engine.py` | 엔진 시작 시 복원, 평가 루프 날짜 경계 체크 |
| `local_server/engine/executor.py` | FILL 로그 메시지 수정 |
| `local_server/storage/log_db.py` | `today_executed_amount()`, `_init_db` 마이그레이션 순서 수정 |
| `local_server/broker/kis/order.py` | `_MOCK_ORDER_TR_ID`, mock TR ID 분기 (주문/취소/미체결) |
