# 아이디어 노트

향후 기능 아이디어 모음. 착수 시 `/spec`으로 발전시킨다.

> 상태: 💡 아이디어 | 📋 spec 있음 | ✅ 구현 완료

## AI 전략 리뷰 시스템 💡

- Claude API로 매매 전략 성과를 분석하고 개선안을 제안받는 기능
- 장 마감 후 거래 결과(승률, 수익률, 손실 종목 패턴)를 Claude에 전달
- Claude가 스코어링 가중치 조정, 지표 추가/제거 등을 제안
- 사용자가 승인/거절하여 전략에 반영 (자동 수정 아님)
- 전용 페이지: `/trading/strategy` (성과 요약, AI 분석 요청, 제안 내역, 승인/거절)
- 참고: `virtual-auto-trading` spec 완료 후 진행

## 로컬 서버 직접 접속 (클라우드 우회) 💡

- 클라우드 서버 장애 시 원격 킬스위치가 불가한 SPOF를 우회하는 방안
- SSH 터널링으로 로컬 서버에 직접 접속하여 제어하는 방식 연구
- 또는 네이티브 앱(모바일/데스크탑)에서 로컬 서버에 직접 연결
- Tailscale/ZeroTier 같은 메시 VPN으로 NAT 뒤 로컬 서버 접근 가능성 검토
- 장기적으로 앱 개발 시 클라우드 relay 없이 직접 연결 옵션 제공 가능
- 보안 리뷰(2026-03-17)에서 원격 킬스위치 SPOF 지적으로 기록

## yfinance 대체/폴백 데이터 소스 💡

- IndicatorProvider가 yfinance에 100% 의존 → yfinance 장애 시 지표 계산 불가
- 폴백 옵션 1: KIS REST API 일봉 조회 (사용자 본인 키, 법적 문제 없음)
- 폴백 옵션 2: 클라우드 서버 일봉 API (이미 yfinance로 수집/저장 중, 약관 검토 필요)
- 배치 요청 최적화: `yf.download(tickers="SYM1 SYM2")` → 호출 수 감소
- 아키텍처 리뷰(2026-03-17)에서 SPOF로 지적

## 데이터 수집 전용 서버 + 시계열 DB 📋

- 전 종목(2,400) 분봉 수집은 API 서버와 분리 필요 (연 ~12GB, ~234M행)
- 수집 봇: 분봉, 뉴스, 커뮤니티, 공시 등을 독립 프로세스로
- 시계열 DB: TimescaleDB (PostgreSQL 확장) 또는 QuestDB
- 20종목까지는 일반 PostgreSQL로 충분, 100+ 넘으면 전환 검토
- 별도 VPS ($10~20/월) 또는 Railway/Fly.io
- 상세: `docs/product/infra-growth-plan.md`

## 대신증권 Creon Plus 연동 📋

- 1분봉 ~2년, 5분봉 ~5년 과거 데이터 제공 (키움보다 김)
- Windows COM API → 배치 수집 스크립트 (`tools/creon_collector.py`)
- 계좌 개설 필요
- 키움과 데이터 중복 구간 정합성 검증 필요

## 분봉 기반 AI 학습 💡

- 분봉 데이터 축적 후 가격 예측 모델 학습 가능
- 일봉 모델 먼저 → 분봉 모델은 6개월+ 데이터 축적 후
- 입력: 분봉 OHLCV + 지표 → 출력: 단기 방향 예측
- 라벨링: N분 후 수익률 기준

## 백테스트 고도화 (v2) 💡

- 멀티 종목 포트폴리오 백테스트
- 파라미터 최적화 (그리드 서치, 베이지안)
- 공매도 / 마진 시뮬레이션
- 워크포워드 분석 (in-sample / out-of-sample 분리)
- 백테스트 전용 워커 (Celery + Redis) — 동시 사용자 10명+ 시

## 뉴스/커뮤니티 센티먼트 수집 💡

- 네이버 증권 종목 토론방, 디시인사이드 주식 갤러리
- 연합인포맥스, 이데일리 등 뉴스 크롤링
- 센티먼트 스코어 → AI 분석 컨텍스트에 주입
- 법적 검토 필요 (크롤링 약관, 저작권)

## AI 자율 전략 탐색 (창발적 전략 생성) 💡

- LLM 에이전트가 자율적으로 전략 생성 → 백테스트 → 결과 분석 → 수정 → 반복
- 사용자가 "삼성전자에 맞는 최적 전략 찾아줘" → AI가 여러 전략을 시도하며 탐색
- 프리미엄 비서의 최종 진화형 (사용자 키 사용)
- 전제조건: AI 코어 서비스 + 프리미엄 비서 + 백테스트 tool 호출
- 2026-03-29 brainstorming에서 도출

## 다른 종목 데이터 참조 💡

- DSL에서 `종목("KOSPI").현재가` 형태로 타 종목/지수 참조
- 활용: "코스피 하락 시 매수 중단", "업종 지수 대비 상대 강도"
- 현재 종목은 `this.` 없이 암묵적 접근 유지
- 구현: 평가기에서 시세 API 호출 + 캐시
- 2026-03-29 brainstorming에서 도출

## DSL 에디터 구문 하이라이팅 (CodeMirror 6) 💡

- textarea → CodeMirror 6 전환으로 DSL 구문 하이라이팅 + 인라인 에러 밑줄
- 커스텀 언어 정의: 키워드, 함수, 필드, 숫자, 연산자, 주석, 화살표 색 구분
- 부수 효과: 줄번호, 탭 들여쓰기, 괄호 매칭, 기존 자동완성을 CM 플러그인으로 교체
- ai-core-service Step 6에서 분리 (2026-03-29)
